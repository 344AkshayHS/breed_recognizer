# breed_recognizer
Breed Recognizer is a machine learning–based system that identifies whether an animal captured through a camera is a cow or a buffalo. It processes the image, extracts visual features like body shape and horns, and uses a trained ML model to classify the animal, helping farmers quickly recognize livestock

project uses:
Deep Learning
Computer Vision
CNN (ResNet50)  

implemented using PyTorch.   
